# Unit tests

Add unit tests here. For example:

- `server/` — backend routes, lib, models
- `admin-app/` — React components and utils
- `widget/` — Preact storefront widget logic

Use your preferred test runner (e.g. Vitest, Jest).
