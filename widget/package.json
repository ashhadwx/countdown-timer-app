{
  "name": "countdown-timer-widget",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "vite build",
    "build:check-size": "vite build && node scripts/check-size.js",
    "preview": "vite preview"
  },
  "dependencies": {
    "preact": "^10.19.0"
  },
  "devDependencies": {
    "@preact/preset-vite": "^2.8.0",
    "vite": "^5.0.0",
    "vite-plugin-compression": "^0.5.1"
  }
}
